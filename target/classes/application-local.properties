# Base de datos local PostgreSQL (sin SSL)
# spring.datasource.url=jdbc:postgresql://localhost:5432/cons_apis
# spring.datasource.username=user_apis
# spring.datasource.password=Ap1Consum02018.*
# spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect



# ==============================================
# Perfil LOCAL - usando H2 en memoria (temporal)
# ==============================================

spring.datasource.url=jdbc:h2:mem:cons_apis;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Consola H2 (opcional - para ver las tablas)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console